services:
{% block services %}
    {{ bundle_underscore }}.link_container:
        class: {{ namespace }}\Container\LinkContainer
        arguments:
          - "@translator.default"
          - "@router"
          - "@zikula_permissions_module.api.permission"
        tags:
          - { name: zikula.link_container }
{% endblock services %}
