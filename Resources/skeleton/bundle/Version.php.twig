<?php

namespace {{ namespace }};

class {{ bundle_name }}Version extends \Zikula_AbstractVersion
{
    public function getMetaData()
    {
        $meta = [];
        $meta['displayname']    = $this->__('{{ vendor }} {{ bundle_name }}');
        $meta['description']    = $this->__('{{ vendor }} {{ bundle_name }} description');
        $meta['url']            = $this->__('{{ bundle_basename|lower }}');
        $meta['version']        = '0.0.1';
        $meta['core_min']       = '1.3.6';
        $meta['securityschema'] = ['{{ bundle }}::' => '::'];

        return $meta;
    }
}